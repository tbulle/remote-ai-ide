apiVersion: v1
kind: Secret
metadata:
  name: remote-ai-ide-secrets
  namespace: lifetracker
  labels:
    app: remote-ai-ide
# Replace with actual token before applying
# AUTH_TOKENS value must be base64-encoded

type: Opaque

data:
  AUTH_TOKENS: ""
