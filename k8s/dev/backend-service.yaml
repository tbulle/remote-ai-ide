apiVersion: v1
kind: Service
metadata:
  name: remote-ai-ide-backend
  namespace: lifetracker
  labels:
    app: remote-ai-ide-dev
    component: backend
spec:
  type: NodePort
  selector:
    app: remote-ai-ide-dev
    component: backend
  ports:
    - name: http
      port: 3002
      targetPort: 3002
      nodePort: 30022
